<?xml version="1.0" encoding="utf-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AmbientSleeper"
             xmlns:converters="clr-namespace:AmbientSleeper.Converters"
             x:Class="AmbientSleeper.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Theme.xaml" />
            </ResourceDictionary.MergedDictionaries>
            <!-- Apply background image to all ContentPages -->
            
            <Style TargetType="ContentPage">
                <Setter Property="BackgroundImageSource" Value="ambient_bg" />
            </Style>
            
            <!-- Ensure layouts don’t paint white over the page background -->
            <Style TargetType="Layout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="Grid">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="StackLayout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="VerticalStackLayout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="HorizontalStackLayout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="ScrollView">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="ContentView">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            
            <!-- Keep Shell chrome transparent so the page bg is visible -->
            <Style TargetType="Shell">
                <Setter Property="Shell.BackgroundColor" Value="Transparent" />
                <Setter Property="Shell.FlyoutBackgroundColor" Value="Transparent" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="Transparent" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.TabBarForegroundColor" Value="White" />
                <Setter Property="Shell.TabBarTitleColor" Value="White" />
                <Setter Property="Shell.TabBarUnselectedColor" Value="#80FFFFFF" />
            </Style>
            
            <!-- Converters -->
            <converters:IntToBoolConverter x:Key="IntToBoolConverter" />
            <converters:TabBackgroundConverter x:Key="TabBackgroundConverter" />
            <converters:InverseBoolConverter x:Key="InverseBoolConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>


<!--<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AmbientSleeper"
             xmlns:converters="clr-namespace:AmbientSleeper.Converters"
             x:Class="AmbientSleeper.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            --><!-- Apply background image to all ContentPages --><!--
            <Style TargetType="ContentPage">
                <Setter Property="BackgroundImageSource" Value="ambient_bg" />
            </Style>

            --><!-- Ensure layouts don’t paint white over the page background --><!--
            <Style TargetType="Layout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="Grid">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="StackLayout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="VerticalStackLayout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="HorizontalStackLayout">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="ScrollView">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>
            <Style TargetType="ContentView">
                <Setter Property="BackgroundColor" Value="Transparent" />
            </Style>

            --><!-- Keep Shell chrome transparent so the page bg is visible --><!--
            <Style TargetType="Shell">
                <Setter Property="Shell.BackgroundColor" Value="Transparent" />
                <Setter Property="Shell.FlyoutBackgroundColor" Value="Transparent" />
                <Setter Property="Shell.TabBarBackgroundColor" Value="Transparent" />
                <Setter Property="Shell.ForegroundColor" Value="White" />
                <Setter Property="Shell.TitleColor" Value="White" />
                <Setter Property="Shell.TabBarForegroundColor" Value="White" />
                <Setter Property="Shell.TabBarTitleColor" Value="White" />
                <Setter Property="Shell.TabBarUnselectedColor" Value="#80FFFFFF" />
            </Style>

            --><!-- Converters --><!--
            <converters:IntToBoolConverter x:Key="IntToBoolConverter" />
            <converters:TabBackgroundConverter x:Key="TabBackgroundConverter" />
        </ResourceDictionary>
    </Application.Resources>
</Application>-->

<!--<Application xmlns="http://schemas.microsoft.com/dotnet/2022/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:AmbientSleeper"
             xmlns:converters="clr-namespace:AmbientSleeper.Converters"
             x:Class="AmbientSleeper.App">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
        <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
        <converters:IntToBoolConverter x:Key="IntToBoolConverter" />
        <converters:TabBackgroundConverter x:Key="TabBackgroundConverter" />
    </ResourceDictionary.MergedDictionaries>
</Application>-->

